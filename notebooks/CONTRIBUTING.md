# 制作一份章节 Notebook

读者只需基础 Python。首次出现 `@tool`、`AIMessage`、`ToolMessage`、`invoke`、State 或服务端 run 时，解释它们在当前实验中的输入、输出和作用。

## 教学骨架

复制 [_template/01-minimal-tool.ipynb](_template/01-minimal-tool.ipynb)，按以下顺序改写：

1. 一项具体问题、学习目标及对应正文。
2. 预期现象：运行后能看见什么，什么情况说明没有完成。
3. 依赖、前置知识和明确的运行模式。
4. 逐步搭建，每段解释紧接相关代码。
5. 读取消息、文件、状态或服务结果，用断言检查实验承诺。
6. 与本章相关的失败或边界；一项“改一个变量再观察”的练习。
7. 结论、清理和下一步。

核心 API、工具定义与关键断言放在 Notebook 中。公共层只提供配置、展示、测试模型和执行辅助；复杂服务可使用章内辅助文件，但说明职责并链接源码。进程启动与清理必须由同一生命周期保护，不能依赖读者记得执行最后一个清理格。

## 模型与验证

- 默认脚本模型需支持真实工具循环，返回 AIMessage，让框架实际执行工具。不能直接构造最终 Agent 状态来冒充执行。
- 同一机制通过公共 `create_model` 切换到 live；live 配置错误必须失败。
- 断言匹配学习目标：工具名称、参数、调用关联、成功状态和结果；文件检查实际内容；异步任务检查实际服务状态。
- `ToolMessage` 存在、打印勾号、模型说“完成”、Notebook 执行无异常，都不能单独证明目标达成。
- 对本来可选的模型自主行为如研究助手是否使用 Todo，按该章约定观察，不强行规定固定答案。
- 区分框架机制、模型选择工具的能力、真实服务及业务产物质量。无 Key 的真实服务实验应明确写出服务要求。

## 提交步骤

1. 与 [#105](https://github.com/datawhalechina/deepagents-in-action/issues/105) 及章节认领 issue 对齐范围，在最新 main 上添加章节目录。
2. 在 `catalog.json` 注册唯一 ID、对应 `scripts/chapters.json` 的 chapter_id、相对路径、依赖组、模型/服务要求和来源 issue；追加 README 条目及正文入口。
3. 沿用锁定环境。需要新依赖时在 `pyproject.toml` 的适当依赖组声明，更新 `uv.lock` 并重跑已收录实验。不要维护多份章节版本清单。
4. 执行 README 的 pytest、静态检查与全量运行命令。修订计算或依赖后从第一格重跑，用 `--write-back` 保存简短、与当前模式一致的输出。
5. 打开生成的 HTML 检查阅读顺序、代码密度与输出折行，确认本地链接和图像可阅读。Markdown/HTML 由执行结果生成，不分别手工维护。
6. 在 PR 中记录源码版本、执行模式、Python/平台、依赖锁及结果；真实模型另写实际模型和服务范围。未执行写 `not_run`，主动跳过写 `skipped`，不要继承旧输出声称新验证通过。

不得提交 Key、Token、`.env`、本机缓存或临时服务状态。正文从 `content/` 修改，`src/content/chapters/` 是生成目录。公共基础设施由维护者协助建设；贡献者优先完成已认领章节的学习目标。

## 自动检查

Notebook CI 在无凭证环境验证目录、链接、共享逻辑和所有已登记实验。基础设施或依赖改动同样全跑。第 6 章一类实验必须启动真实本地服务；真实模型验证由维护者明确选择并记录，不向普通 fork PR 提供凭证。
